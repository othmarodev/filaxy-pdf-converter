# Filaxy PDF Converter — engine dependencies (free / open source)
# NOTE: PyMuPDF is pinned. Newer releases removed Rect.get_area(), which
# pdf2docx still calls; 1.23.26 is the last fully-compatible line.
pdf2docx==0.5.8
PyMuPDF==1.23.26
python-docx==1.2.0
Pillow>=10.0
