# Filaxy PDF Converter — engine dependencies (free / open source)
# The coordinate-mapping engine (coordmap.py) needs only these:
#   PyMuPDF  — PDF parsing + rasterisation (pinned: 1.23.26 is the validated line)
#   Pillow   — image handling for the page-background layer
#   fonttools — read/obfuscate fonts for embedding into the .docx (ECMA-376)
PyMuPDF==1.23.26
Pillow>=10.0
fonttools>=4.0
