# Sample documents

Personal/real PDFs are NOT stored here — they contain private data and would
leak into the public repo. Place test PDFs in `tests/fixtures/` (gitignored)
and run `python tests/test_samples_local.py`.
